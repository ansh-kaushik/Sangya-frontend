import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1d2c94",
    },
    secondary: {
      main: "#318a34",
      light: "#a1e2a4",
    },
  },
});

export default theme;
